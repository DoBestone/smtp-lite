root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp/server cmd/server/main.go"
  bin = "tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "frontend", "benchmark", "scripts", "web"]
  delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
